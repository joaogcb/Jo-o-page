function updateDropdown() {
    // if is cursor over .login, remove class .hidden from #dropdown
    // else add class .hidden to #dropdown

    let dropdown = document.getElementById("dropdown");
    let login = document.querySelector(".login");
    let dom = login.getBoundingClientRect();


}